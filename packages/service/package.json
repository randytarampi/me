{
  "name": "@randy.tarampi/service",
  "description": "The backend for www.randytarampi.ca",
  "version": "4.0.12",
  "author": {
    "name": "Randy Tarampi",
    "email": "rt@randytarampi.ca",
    "url": "https://www.randytarampi.ca"
  },
  "bugs": "https://github.com/randytarampi/me/issues",
  "dependencies": {
    "@randy.tarampi/js": "4.0.12",
    "@randy.tarampi/lambda-logger": "4.0.12",
    "@randy.tarampi/serverless": "4.0.12",
    "big.js": "5.2.2",
    "dynamoose": "1.7.3",
    "flickr-sdk": "3.9.0",
    "form-data": "2.3.3",
    "immutable": "github:randytarampi/immutable-js#me-release",
    "instagram-api": "1.0.3",
    "isomorphic-fetch": "2.2.1",
    "js-yaml": "3.13.1",
    "lodash": "4.17.11",
    "luxon": "1.15.0",
    "mime-types": "2.1.23",
    "oauth": "0.9.15",
    "pad-right": "0.2.2",
    "query-string": "6.5.0",
    "rss": "1.2.2",
    "tumblr.js": "2.0.0",
    "twitter": "1.7.1",
    "unsplash-js": "5.0.0"
  },
  "devDependencies": {
    "@babel/cli": "7.4.3",
    "gulp": "4.0.2",
    "mocha": "6.1.4",
    "nyc": "14.1.1",
    "serverless": "1.42.3",
    "webpack": "4.32.2",
    "webpack-cli": "3.3.2"
  },
  "directories": {
    "lib": "./src/lib",
    "test": "./test"
  },
  "engines": {
    "node": ">=8"
  },
  "license": "MIT",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "directory": "me/packages/service",
    "type": "git",
    "url": "https://github.com/randytarampi/me.git"
  },
  "scripts": {
    "clean": "gulp -LLLL --color clean",
    "cover": "rm -rf coverage/ .nyc_output/; npm run pretest:dynamodb-local && NODE_ENV=test nyc gulp -LLLL --color test && npm run kill:dynamodb-local",
    "deploy": "if [ -z \"$NODE_ENV\" ]; then NODE_ENV=dev npm run deploy; else npm run deploy:$NODE_ENV; fi; ",
    "deploy:dev": "sls create_domain --stage $NODE_ENV --region us-east-1 && sls deploy --conceal --stage $NODE_ENV --region us-east-1",
    "deploy:prd": "sls create_domain --stage $NODE_ENV --region ca-central-1 && sls deploy --conceal --stage $NODE_ENV --region ca-central-1",
    "dev": "npm run dev:serverless",
    "dev:serverless": "sls offline start",
    "kill": "npm run kill:dynamodb-local",
    "kill:dynamodb-local": "if [ -f /tmp/ca.randytarampi.me.service.dynamo.pid ]; then echo \"Killing dynamodb-local (`cat /tmp/ca.randytarampi.me.service.dynamo.pid`) from /tmp/ca.randytarampi.me.service.dynamo.pid\"; kill -9 `cat /tmp/ca.randytarampi.me.service.dynamo.pid`; rm /tmp/ca.randytarampi.me.service.dynamo.pid; fi;",
    "poststart": "npm run kill",
    "posttest": "npm run kill",
    "prepare": "sls dynamodb install",
    "prestart": "npm run kill",
    "pretest": "gulp -LLLL --color lint",
    "pretest:dynamodb-local": "npm run kill:dynamodb-local && npm run start:dynamodb-local && echo \"Started dynamodb-local (`cat /tmp/ca.randytarampi.me.service.dynamo.pid`) from /tmp/ca.randytarampi.me.service.dynamo.pid\";",
    "preuninstall": "npm run preuninstall:dynamodb && npm run clean",
    "preuninstall:dynamodb": "sls dynamodb remove",
    "start": "npm run dev",
    "start:dynamodb-local": "npm run start:dynamodb-local:start && npm run start:dynamodb-local:migrate",
    "start:dynamodb-local:migrate": "sls dynamodb migrate --stage local",
    "start:dynamodb-local:start": "java -Djava.library.path=./.dynamodb/DynamoDBLocal_lib -jar ./.dynamodb/DynamoDBLocal.jar -inMemory & echo $! > /tmp/ca.randytarampi.me.service.dynamo.pid",
    "test": "npm run pretest:dynamodb-local && npm run test:actual",
    "test:actual": "NODE_ENV=test gulp -LLLL --color test"
  }
}
