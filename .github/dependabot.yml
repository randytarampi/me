version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      dev-dependencies:
        patterns:
          - "*"
        dependency-type: development
      production:
        patterns:
          - "*"
        dependency-type: production
