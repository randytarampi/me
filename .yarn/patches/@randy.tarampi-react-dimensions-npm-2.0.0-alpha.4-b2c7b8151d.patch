diff --git a/index.js b/index.js
index 8e1fd2af479bcb3223b6807079822c27c7e510a3..812484a59f0c23171a7f58810b1bb8eaede8f27d 100644
--- a/index.js
+++ b/index.js
@@ -222,12 +222,17 @@ function Dimensions() {
             console.warn('Wrapper div has no height or width, try overriding style with `containerStyle` option');
           }
 
+          // NOTE-RT: React 19 removed string refs (`this.refs`), so use callback refs that populate a plain
+          // NOTE-RT: `this.refs`-shaped object the rest of this HOC still reads.
+          var _self = this;
+          _self.refs = _self.refs || {};
+
           return React.createElement(
             'div',
-            { style: containerStyle, ref: 'wrapper' },
+            { style: containerStyle, ref: function (node) { _self.refs.wrapper = node; } },
             containerWidth || containerHeight ? React.createElement(ComposedComponent, _extends({}, this.state, this.props, {
               updateDimensions: this.updateDimensions,
-              ref: 'wrappedInstance'
+              ref: function (node) { _self.refs.wrappedInstance = node; }
             })) : null
           );
         }
